<Window x:Class="Warehouse_Management.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Warehouse_Management.View"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:Warehouse_Management.ViewModel"
        xmlns:dragablz="http://dragablz.net/winfx/xaml/dragablz"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        Title="Warehouse Manager" Height="1000" Width="800"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <dragablz:TabablzControl>
        <dragablz:TabablzControl.InterTabController>
            <dragablz:InterTabController />
        </dragablz:TabablzControl.InterTabController>
        <TabItem Header="Map" DataContext="{Binding Path=MapVM}">
            <local:MapControl MapImage="{Binding MapImage}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
        </TabItem>
        <TabItem Header="Warehouses">
            <StackPanel  Height="500" Orientation="Vertical">
                <ListBox FontSize="25" Padding="25" Foreground="#013284">
                    [lista magazynów]
                </ListBox>
            </StackPanel>
        </TabItem>

    </dragablz:TabablzControl>
</Window>