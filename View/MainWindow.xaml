<Window x:Class="Warehouse_Management.View.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:dragablz="http://dragablz.net/winfx/xaml/dragablz" xmlns:local="clr-namespace:Warehouse_Management.View"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Warehouse_Management.ViewModel" Title="Warehouse Manager" Width="800" Height="1000"
        Background="{DynamicResource MaterialDesignPaper}" FontFamily="{DynamicResource MaterialDesignFont}"
        TextElement.FontSize="13" TextElement.FontWeight="Regular"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}" TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto" mc:Ignorable="d">
    <dragablz:TabablzControl>
        <dragablz:TabablzControl.InterTabController>
            <dragablz:InterTabController />
        </dragablz:TabablzControl.InterTabController>
        <TabItem DataContext="{Binding Path=MapVM}" Header="Map">
            <local:MapControl HorizontalAlignment="Left" VerticalAlignment="Top" MapImage="{Binding MapImage}"
                    MapItems="{Binding MapItems}" />
        </TabItem>
        <TabItem Header="Warehouses">
            <StackPanel Height="500" Orientation="Vertical">
                <ListBox Padding="25" FontSize="25" Foreground="#013284">
                    [lista magazynów]
                </ListBox>
            </StackPanel>
        </TabItem>
    </dragablz:TabablzControl>
</Window>