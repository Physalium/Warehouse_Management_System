<UserControl
    x:Class="Warehouse_Management.View.SupplyPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Warehouse_Management.View"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Warehouse_Management.ViewModel.EntitiesVM"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">  
                <Grid>
                    <Grid.Resources>
                        <SolidColorBrush x:Key="brush" Color="White"/>
                    </Grid.Resources>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*" />
                        <RowDefinition Height="10*" />
                        <RowDefinition Height="1*" />
                    </Grid.RowDefinitions>
                    <Border Grid.ColumnSpan="3" Grid.Row="0" BorderBrush="{StaticResource brush}" BorderThickness="1 1 1 1">
                        <TextBlock> Tutaj bydzie lista z Warehousami</TextBlock>
                    </Border>
                    <Border Grid.Column="0" Grid.Row="1" BorderBrush="{StaticResource brush}" BorderThickness="1 1 1 1">
                        <TextBlock> TIRY </TextBlock>
                    </Border>
                    <Border Grid.Column="1" Grid.Row="1" BorderBrush="{StaticResource brush}" BorderThickness="1 1 1 1">
                        <TextBlock> NACZEPY </TextBlock>
                    </Border>
                    <Border Grid.Column="2" Grid.Row="1" BorderBrush="{StaticResource brush}" BorderThickness="1 1 1 1">
                        <TextBlock> PRODUKTY </TextBlock>
                    </Border>
                    <Border Grid.ColumnSpan="3" Grid.Row="2" BorderBrush="{StaticResource brush}" BorderThickness="1 1 1 1">
                        <TextBlock> ADD BUTTON </TextBlock>
                    </Border>
                </Grid>
</UserControl>